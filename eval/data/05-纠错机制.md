# 纠错机制

相关性评分采用宽松标准：只过滤明显不相关的内容，避免误删相关文档。

当检索到的片段被全部判定为不相关时，系统触发查询改写，将问题改写成更适合搜索的形式，然后调用 Tavily 网络搜索补充本地文档缺少的信息。

纠错机制的主要目的是避免直接使用不相关的内容生成答案，从而降低幻觉概率。

对外部服务的网络调用（向量化、写入、检索、Tavily）都带自动重试：连接中断或超时类错误最多重试 4 次，指数退避 2 到 15 秒；参数错误和鉴权错误不重试。

入库阶段有进度条（按来源和已向量化片段数显示），回答阶段有按步骤推进的进度条。
