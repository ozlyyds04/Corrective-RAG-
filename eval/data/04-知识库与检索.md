# 知识库与检索

所有知识库共用同一个 Qdrant 集合 rag-qdrant，每条向量数据带 kb 标签用于区分所属知识库。

按知识库过滤检索或删除数据时，kb 字段必须建 keyword 类型的 payload 索引，否则 Qdrant 会返回 400 错误。

知识库可以在界面中新建、选择、删除；删除知识库只删除该库的数据，不影响其他知识库；默认知识库不可删除。

知识库名称列表保存在项目根目录的 knowledge_bases.json 中。

检索时先按当前知识库的 kb 标签过滤，再在过滤结果中做向量相似度搜索。
